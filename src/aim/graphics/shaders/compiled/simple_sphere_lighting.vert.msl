#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct type_UniformBlock
{
    float4 BaseColor;
    float4 LightColor;
    float4 LightDirection;
    float4x4 MatrixTransform;
};

struct main0_out
{
    float4 out_var_COLOR0 [[user(locn0)]];
    float4 gl_Position [[position]];
};

struct main0_in
{
    float3 in_var_POSITION0 [[attribute(0)]];
};

vertex main0_out main0(main0_in in [[stage_in]], constant type_UniformBlock& UniformBlock [[buffer(0)]])
{
    main0_out out = {};
    float4 _54 = fast::clamp(((UniformBlock.LightColor * fast::clamp(dot(in.in_var_POSITION0, UniformBlock.LightDirection.xyz), 0.0, 1.0)) * (1.0 - UniformBlock.BaseColor.w)) + (UniformBlock.BaseColor * UniformBlock.BaseColor.w), float4(0.0), float4(1.0));
    _54.w = 1.0;
    out.gl_Position = UniformBlock.MatrixTransform * float4(in.in_var_POSITION0, 1.0);
    out.out_var_COLOR0 = _54;
    return out;
}

