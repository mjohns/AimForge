#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct type_UniformBlock
{
    uint InstanceOffset;
};

struct InstanceData
{
    float4x4 MatrixTransform;
    float4 Color;
};

struct type_StructuredBuffer_InstanceData
{
    InstanceData _m0[1];
};

struct main0_out
{
    float4 out_var_COLOR [[user(locn0)]];
    float4 gl_Position [[position]];
};

struct main0_in
{
    float3 in_var_POSITION0 [[attribute(0)]];
};

vertex main0_out main0(main0_in in [[stage_in]], constant type_UniformBlock& UniformBlock [[buffer(0)]], const device type_StructuredBuffer_InstanceData& InstanceBuffer [[buffer(1)]], uint gl_InstanceIndex [[instance_id]])
{
    main0_out out = {};
    uint _34 = UniformBlock.InstanceOffset + gl_InstanceIndex;
    out.gl_Position = InstanceBuffer._m0[_34].MatrixTransform * float4(in.in_var_POSITION0, 1.0);
    out.out_var_COLOR = InstanceBuffer._m0[_34].Color;
    return out;
}

